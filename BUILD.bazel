genrule(
    name = "docs",
    srcs = ["mydocs.tmpl"],
    outs = ["docs.rst"],
    cmd = "cp $< $@",
)

sh_binary(
    name = "install_sphinx_sources",
    srcs = ["install_sphinx_sources.sh"],
    args = ["$(rootpath :docs)"],
    data = [":docs"],
)
