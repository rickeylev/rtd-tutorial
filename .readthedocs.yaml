version: 2

build:
  os: "ubuntu-22.04"
  tools:
    python: "3.11"
    nodejs: "19"
  apt_packages:
    - golang
  jobs:
    pre_build:
      - go install github.com/bazelbuild/bazelisk@latest
      - npm install -g @bazel/bazelisk
      - find .
      - echo hi 2>&1
      - which bazel
      - which bazelisk
      - bazel help
